version: 2

models:
  - name: user_top_tracks_short_term
    description: User's top tracks, short term.
    columns:
      - name: track_id
        description: Track ID
        tests:
          - unique
          - not_null
      - name: artist_name
        description: Track artist name(s)
        tests:
          - not_null
      - name: rank
        description: Rank of the returned record
        tests:
          - unique
          - not_null

  - name: user_top_tracks_medium_term
    description: User's top tracks, medium term.
    columns:
      - name: track_id
        description: Track ID
        tests:
          - unique
          - not_null
      - name: artist_name
        description: Track artist name(s)
        tests:
          - not_null
      - name: rank
        description: Rank of the returned record
        tests:
          - unique
          - not_null

  - name: user_top_tracks_long_term
    description: Users's top tracks, long term.
    columns:
      - name: track_id
        description: Track ID
        tests:
          - unique
          - not_null
      - name: artist_name
        description: Track artist name(s)
        tests:
          - not_null
      - name: rank
        description: Rank of the returned record
        tests:
          - unique
          - not_null

  - name: global_viral_tracks_daily
    description: Global viral tracks daily.
    columns:
      - name: track_id
        description: Track ID
        tests:
          - unique
          - not_null
      - name: artist_name
        description: Track artist name(s)
        tests:
          - not_null
      - name: rank
        description: Rank of the returned record
        tests:
          - unique
          - not_null

  - name: global_top_tracks_daily
    description: Global top tracks daily
    columns:
      - name: track_id
        description: Track ID
        tests:
          - unique
          - not_null
      - name: artist_name
        description: Track artist name(s)
        tests:
          - not_null
      - name: rank
        description: Rank of the returned record
        tests:
          - unique
          - not_null

  - name: global_top_tracks_weekly
    description: Global top tracks weekly
    columns:
      - name: track_id
        description: Track ID
        tests:
          - unique
          - not_null
      - name: artist_name
        description: Track artist name(s)
        tests:
          - not_null
      - name: rank
        description: Rank of the returned record
        tests:
          - unique
          - not_null

  - name: user_top_artists_short_term
    description: User top artists, short term.
    columns:
      - name: artist_id
        description: Artist ID
        tests:
          - unique
          - not_null
      - name: artist_name
        description: Track artist name(s)
        tests:
          - not_null
      - name: rank
        description: Rank of the returned record
        tests:
          - unique
          - not_null

  - name: user_top_artists_medium_term
    description: User top artists, medium term.
    columns:
      - name: artist_id
        description: Artist ID
        tests:
          - unique
          - not_null
      - name: artist_name
        description: Track artist name(s)
        tests:
          - not_null
      - name: rank
        description: Rank of the returned record
        tests:
          - unique
          - not_null

  - name: user_top_artists_long_term
    description: User top artists, long term
    columns:
      - name: artist_id
        description: Artist ID
        tests:
          - unique
          - not_null
      - name: artist_name
        description: Track artist name(s)
        tests:
          - not_null
      - name: rank
        description: Rank of the returned record
        tests:
          - unique
          - not_null

  - name: vw_top_listened_to_genre_long_term
    description: User top genres listened to, long term
    columns:
      - name: genre
        description: Music Genre
        tests:
          - unique
          - not_null
